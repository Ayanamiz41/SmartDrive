server:
  port: 8082
  servlet:
    context-path: /api

spring:
  application:
    name: smartdrive-file
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/smartdrive?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowPublicKeyRetrieval=true&allowMultiQueries=true
    username: root
    password: kksk114514
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCP
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
  data:
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      timeout: 2000
      lettuce:
        pool:
          max-active: 20
          max-wait: -1
          max-idle: 10
          min-idle: 0
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB
  elasticsearch:
    uris: http://localhost:9200
    connection-timeout: 3s
    socket-timeout: 30s
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.smartdrive.file.entity
  configuration:
    map-underscore-to-camel-case: true

project:
  folder: D:/project/SmartDrive/data

# AI 摘要配置（使用 OpenAI 兼容接口）
ai:
  summary:
    enabled: true
    api-key: sk-7f28432df18145858e130ddc3618b8a5
    base-url: https://api.deepseek.com/v1
    model: deepseek-v4-flash
