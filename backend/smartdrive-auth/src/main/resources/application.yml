server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: smartdrive-auth
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/smartdrive?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowPublicKeyRetrieval=true&allowMultiQueries=true
    username: root
    password: kksk114514
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCP
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
  data:
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      timeout: 2000
      lettuce:
        pool:
          max-active: 20
          max-wait: -1
          max-idle: 10
          min-idle: 0
  mail:
    host: smtp.qq.com
    port: 465
    username: 1066973476@qq.com
    password: thavvlgjnoipbeai
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
        debug: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        import-check:
          enabled: false
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        shared-configs:
          - data-id: smartdrive-common.yaml
            group: DEFAULT_GROUP
            refresh: true

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.smartdrive.auth.entity
  configuration:
    map-underscore-to-camel-case: true

# JWT
jwt:
  secret: smartdrive-jwt-secret-key-2026-must-be-at-least-256-bits-long-change-in-production
  expiration: 1800000
  refresh-expiration: 604800000

# 项目路径
project:
  folder: D:/project/SmartDrive/data

# 管理员邮箱
admin:
  emails: 1066973476@qq.com

# 开发模式：跳过验证码校验
app:
  dev: false
